[project]
name = "benchmarks"
version = "0.1.0"
layout = "maven"

[toolchains]
java = "21"

# [bench] is optional — jot pins JMH 1.37 and uses src/bench/java because layout = "maven".
# Uncomment to override jmh-version or add extra deps:
# [bench]
# jmh-version = "1.37"
# deps = []
